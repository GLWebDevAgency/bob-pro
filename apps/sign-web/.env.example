# URL publique du backend NestJS exposant les endpoints publics de signature (/public/sign/:token).
# Production Vercel : URL https de l'API Railway. Ne jamais laisser localhost en production.
NEXT_PUBLIC_API_URL=https://api.bobpro.fr
