import type { ReactNode } from 'react';

export const metadata = {
  title: 'Bob Pro — Confirmation du compte',
};

export default function AuthConfirmLayout({ children }: { children: ReactNode }) {
  return children;
}
