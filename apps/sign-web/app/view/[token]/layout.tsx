import type { ReactNode } from 'react';

export const metadata = {
  title: 'Bob Pro — Consultation de document',
};

export default function ViewLayout({ children }: { children: ReactNode }) {
  return children;
}
