{
  "name": "@bob/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3010",
    "build": "pnpm --filter @bob/core build && pnpm --filter @bob/i18n build && next build",
    "start": "next start -p 3010",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint . --ext .ts,.tsx --env browser"
  },
  "dependencies": {
    "@bob/core": "workspace:*",
    "@bob/i18n": "workspace:*",
    "next": "^16.0.0",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.6.3"
  }
}
