/**
 * @bob/ui — primitives UI partagées (Button, Card, Chip, Segmented, Badge, ListRow,
 * ScoreBar, Sheet, Toast, FAB, TabBar, SideNav, Stepper, SignaturePad, Avatar,
 * MoneyText, Eyebrow, SectionHeader) en `.native.tsx` / `.web.tsx`.
 * C00 ne pose que la coque du package : aucune primitive avant la double-signature de C03.
 */

export {};
