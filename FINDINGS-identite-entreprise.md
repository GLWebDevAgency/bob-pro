
======================================================================
REVUE 1
======================================================================

### [CRITIQUE] packages/ai/src/agent/classifier.ts
VOIX (point 6) — « chercher_entreprise » et « creer_client » ne sont JOIGNABLES PAR AUCUN CHEMIN. Le commit 2a9952d9 annonce « donner l'annuaire à Bob » mais l'outil n'est décrit à aucun modèle et n'est dispatché nulle part. La seule liste de schémas exposée au LLM (LLM_TOOL_SPECS, classifier.ts) compte 42 outils et ne contient ni l'un ni l'autre ; BobIntent (agent/intent.ts) n'a aucune intention de création de client ; bob-agent.ts appelle this.tool('…') pour 20+ outils, jamais pour ces deux-là. La phrase fondatrice « ajoute un client, la boulangerie Martin à Fécamp » ne peut donc pas aboutir. Le test registry.test.ts ne prouve que la présence de l'outil dans le tableau buildBobTools, pas sa joignabilité. Doctrine 2 (papa vocal) et 3 (parité d'actions) non tenues : le geste existe au doigt, pas à la voix.

PREUVE : git grep -rn "creer_client|chercher_entreprise" packages/ai/src apps/api/src apps/mobile/src (hors tests) => 3 occurrences seulement, toutes des DÉFINITIONS ou des commentaires : registry.ts:1412, registry.ts:1451, actions.ts:564/572, backend.service.ts:4996/4998/5029. `grep -c "^    name: '" packages/ai/src/agent/classifier.ts` => 42, aucun des deux noms dans la liste. `grep -n "this.tool('" packages/ai/src/agent/bob-agent.ts` => aucun 'creer_client' ni 'chercher_entreprise'. Comparaison de contrôle : 'chercher_document' est, lui, présent dans classifier.ts:250 ET dispatché.

### [MAJEURE] packages/core/src/application/company/search-company.ts
AMBIGUÏTÉ (point 3) — orderCompanySearchCandidates trie par RANG D'ENTREPRISE d'abord (a.company - b.company) puis tronque à 8. Un seul SIREN portant ≥8 établissements correspondants occupe donc les 8 places et FAIT DISPARAÎTRE toutes les autres entreprises homonymes, sans que rien ne le dise. Pire : l'écran annonce « 8 établissement(s) trouvé(s). Sélectionnez le bon. » (clients.createCompanyResultsCount) — une AFFIRMATION FAUSSE, l'amont en avait rendu 18 sur 6 entreprises. C'est exactement le scénario cible : la vraie boulangerie Martin de Fécamp est invisible parce qu'une chaîne homonyme sort en tête. Doctrine 8 : ni refus nommé, ni troncature dite.

PREUVE : Sonde exécutée dans apps/api (fetch stubbé, réponse amont = 1 entreprise « BOULANGERIE MARTIN (chaîne) » avec 12 matching_etablissements + 5 entreprises « BOULANGERIE MARTIN i » à Fécamp) → SearchCompaniesByName.execute({query:'boulangerie martin'}) rend 8 rangées, SIREN distincts rendus = ['111111111'] : les 5 autres entreprises ont disparu, aucun message. Sortie brute : 8 lignes toutes 'BOULANGERIE MARTIN (chaîne) / PARIS / 111111111000xx'.

### [MAJEURE] apps/mobile/src/components/company-search-field.logic.ts
AMBIGUÏTÉ (point 3) — companyRowMeta ne compose que [code postal + ville, forme juridique|NAF, SIRET]. La RUE est présente dans le candidat (address.line1, construite par buildEtablissementAddress à partir de numero_voie/type_voie/libelle_voie) et volontairement jetée. Deux établissements d'une même enseigne dans la même ville — ou deux homonymes de même forme juridique — donnent DEUX RANGÉES IDENTIQUES au SIRET près. La ligne primaire est identique (la dénomination vient du niveau ENTREPRISE, r.nom_complet, donc partagée par tous les établissements). Aggravant : SuggestionRowView rend la ligne secondaire en numberOfLines={1} ; à fort grossissement de texte, le SIRET — seul discriminant restant — est tronqué et les deux rangées deviennent littéralement indistinguables.

PREUVE : Sonde exécutée dans apps/mobile : deux candidats identiques sauf siret (…01021 / …01039) et line1 ('12 AVENUE JEAN JAURES' / '3 ROUTE DE SAINT-LEONARD'), même ville 76400 FECAMP. Rangée 1 = 'CARREFOUR HYPERMARCHES · établissement' / '76400 FECAMP · SAS · 451 321 335 01021' ; rangée 2 = 'CARREFOUR HYPERMARCHES · établissement' / '76400 FECAMP · SAS · 451 321 335 01039'. Assertions vertes : ligne primaire strictement égale, méta égale une fois les chiffres retirés, aucune des deux rues n'apparaît.

### [MAJEURE] apps/mobile/app/client/[id].tsx
Le SIRET d'un client ne peut PLUS JAMAIS être retiré, et la copy dit le contraire. CustomerForm.submit() n'émet `siret` que si `siret.length === 14` ; vider le champ le fait donc disparaître du payload — mais submitEdit fait `patch: { ...preserved, ...payload }` avec `preserved.siret = customer.siret`, qui reprend la main. Or clients.createSiretUnverifiedBlock dit explicitement « vide le champ pour continuer sans lui » : l'instruction que Bob donne ne marche pas. Corollaire : basculer une fiche b2b en « particulier » n'émet ni siren ni siret ni TVA, mais `preserved` réinjecte le SIRET → Customer.of (customer.ts, `siren ??= parsedSiret.value.siren().value`) RE-DÉRIVE un SIREN et la fiche « particulier » conserve une identité légale que le formulaire b2c n'affiche même pas — donc irrécupérable.

PREUVE : customer-form.tsx:571 `...(type !== 'b2c' && siret.length === 14 && !siretCandidateUnverified ? { siret } : {})` ; customer-form.tsx:742 `setSiretCandidateUnverified(next.length > 0)` (champ vidé ⇒ false ⇒ Enregistrer actif) ; client/[id].tsx:655 `...(customer.siret != null ? { siret: customer.siret } : {})` puis :666 `{ id, patch: { ...preserved, ...payload } }`. Aucune clé `siret` dans payload ⇒ preserved gagne. Entrée exacte : fiche b2b avec SIRET 45132133501021 → éditer → vider le champ SIRET → Enregistrer → le SIRET est toujours là.

### [MAJEURE] apps/mobile/src/components/customer-form.tsx
Choisir une proposition EFFACE silencieusement un n° de TVA intracom saisi à la main. resolveCompanyIdentityPatch rend `tvaIntracom: ''` quand la source n'en fournit pas (cas fréquent : r.tva absent/null pour un établissement), et applyIdentityPatch fait `setTvaIntracom(patch.tvaIntracom)` SANS CONDITION. En édition, si le SIREN est identique il n'y a même pas de confirmation (replacesExistingIdentity = false), et submitEdit ne préserve PAS tvaIntracom (contrairement à siret/paymentTerms/billingChannel). Résultat : l'artisan qui choisit son établissement pour corriger l'adresse perd le n° de TVA du client — champ obligatoire sur facture B2B (art. 289 II CGI, que le LegalHint du même écran rappelle). Le lot connaît pourtant la règle : le commentaire de onChangeText du nom dit « éditer le TEXTE ne touche NI le SIREN, NI le SIRET, NI la TVA … perte de donnée silencieuse » — applyIdentityPatch la viole.

PREUVE : company-search-field.logic.ts:213-218 `const vat = source.tvaIntracom?.trim() ? … : null; tvaIntracom: vat !== null && vat.ok ? vat.value : ''` ; customer-form.tsx:378 `setTvaIntracom(patch.tvaIntracom)`. Sonde : resolveCompanyIdentityPatch({siren:'451321335', siret:'45132133501021', denomination:'…', tvaIntracom:null, …}) ⇒ patch.tvaIntracom === '' et tvaUnverified === false (aucune alerte). client/[id].tsx:654-661 : `preserved` contient siret, paymentTermsLabel, paymentTerms, billingChannel, isInternational, isSubcontractingBtp — jamais tvaIntracom.

### [MOYENNE] apps/mobile/src/components/customer-form.tsx
QUOTA (point 2) — le débounce tient sur la frappe (12 caractères ⇒ 1 seul appel amont, vérifié), mais L'ANNULATION N'EST PAS EFFECTIVE sur le chemin de validation clavier. onSubmitEditing appelle runCompanySearch directement ; le seul clearTimeout est le cleanup de l'effet, dont les dépendances sont [searchState.query, searchState.locked, type] — or runCompanySearch ne dispatche qu'un `issue`, qui ne change QUE `issued`, donc aucune dépendance ne bouge, le cleanup ne s'exécute pas et le timer débounce tire quand même. Taper puis valider = 2 appels amont pour un seul geste ; marteler la touche « rechercher » contourne totalement le débounce (aucun étranglement client sur ce chemin) — 10 appels en quelques secondes, sur un quota amont de 7 req/s PARTAGÉ par tous les tenants.

PREUVE : Sonde à répliques fidèles de l'effet (fake timers, comparaison de dépendances Object.is comme React) : (a) 12 caractères tapés à 80 ms d'intervalle ⇒ appels = ['boulangerie'] (1) ; (b) type('carrefour') puis submitEditing à t+100 ms ⇒ appels = ['carrefour','carrefour'] (2) ; (c) 1 frappe + 9 validations ⇒ 10 appels amont.

### [MOYENNE] apps/mobile/src/components/customer-form.tsx
ÉTAT MUET (point 4) — l'effet de recherche est indexé sur le TEXTE, pas sur une intention. Redemander la MÊME requête ne relance rien : le reducer `edit` remet outcome=null et settled=issued (donc companySearchView rend { kind: 'idle' }, qui n'affiche STRICTEMENT RIEN — ni liste, ni « aucun résultat », ni erreur, ni squelette), tandis que useEffect voit une dépendance inchangée et ne planifie aucun appel. La liste affichée disparaît et rien ne la remplace, définitivement. Chemin réel : CompanyIdentityReviewPanel.onSearchByName → dispatchSearch({type:'edit', text: review.searchQuery}) — si le champ porte déjà ce texte (relecture d'une seconde photo du même en-tête, ou nom déjà tapé), le bouton « chercher ce nom à l'annuaire » est MORT et silencieux. Doctrine 8 : refus silencieux.

PREUVE : Sonde : type('mercier plomberie') → 1 appel, réponse [] → vue {kind:'empty'} ; puis editSameText('mercier plomberie') + 2000 ms → appels toujours 1, vue = {kind:'idle'} (rien de rendu). Code : customer-form.tsx:364 deps [searchState.query, searchState.locked, type] ; company-search-field.logic.ts:75 (edit ⇒ outcome:null) et :116 (`if (state.outcome === null) return { kind: 'idle' }`) ; CompanyIdentityReviewPanel.tsx:226 onPress={() => onSearchByName(review.searchQuery ?? '')}.

### [MOYENNE] apps/mobile/src/components/company-search-field.logic.ts
Refus MAL NOMMÉ (doctrine 8) — companySearchErrorKey mappe `rate_limited` ET le repli `dependency` sur clients.createCompanySearchDown : « L'annuaire officiel est momentanément indisponible… Réessayez ». Or le 429 le plus probable vient de NOTRE PROPRE throttle (@Throttle 30/min sur GET /company/search) : le corps d'une ThrottlerException ne décode pas en AppError, http-client retombe sur { kind:'dependency', port:'api', cause:'HTTP 429' } et l'utilisateur s'entend affirmer qu'un TIERS est en panne alors qu'il ne l'est pas — et le remède proposé (« réessayez ») échouera encore pendant une minute. Aucune copy ne nomme « tu vas trop vite, attends N secondes », alors que retryAfterSeconds existe dans le type AppError rate_limited.

PREUVE : company-search-field.logic.ts:139-151 (dependency|unavailable|rate_limited → 'clients.createCompanySearchDown') ; api.controllers.ts:2382 @Throttle({default:{limit:30,ttl:60000}}) ; http-client.ts:2097 `: { kind: 'dependency', port: 'api', cause: `HTTP ${res.status}` }` (le corps Nest {statusCode,message} ne passe aucun décodeur AppError). i18n : « L'annuaire officiel est momentanément indisponible : aucun résultat ne peut être affirmé. Réessayez ou saisissez manuellement. »

### [MOYENNE] apps/mobile/src/components/customer-form.tsx
CONTRASTE AA NON TENU (point 7) sur un état REQUIS. La phrase d'affordance permanente ET l'état « trop court » (clients.createCompanySearchTooShort, « 3 caractères minimum ») sont rendus en colors.slate400 (#8A99A8) sur surface : ratio mesuré 2,92:1 — sous les 4,5:1 exigés pour du texte normal (font('sub') = 13,5 px), et même sous 3:1. Sur fond d'app : 2,60:1. Le token slate400 est d'ailleurs mappé 'nonContent.disabled' dans le système de tokens : une couleur de NON-CONTENU utilisée pour porter un des cinq états. Le lot a pourtant enrichi le test de contraste (packages/tokens/src/index.test.ts) — il y a certifié danger et succès, pas la couleur qu'il vient d'employer pour un état. Aggravant secondaire : la mention « Fermé » (semantic.danger) tombe à 4,26:1 quand la rangée est pressée ou au focus clavier (backgroundColor: colors.bg), paire non certifiée par le test.

PREUVE : Sonde WCAG (même formule que packages/tokens/src/index.test.ts) : slate400 #8A99A8 sur surface #FFFFFF => 2.92 ; sur bg #EFF2F7 => 2.60 ; slate500 => 5.48 (conforme) ; semantic.danger sur bg => 4.26. Usage : customer-form.tsx:647 `<Text style={[font('sub'), { color: colors.slate400 }]}>` rendant createCompanySearchTooShort / createCompanySearchHint ; customer-form.tsx:181 `backgroundColor: pressed || focused ? colors.bg : 'transparent'` sous le mark danger (:189).

### [MOYENNE] apps/mobile/src/components/customer-form.tsx
A11Y (point 7) — l'annonce du nombre de résultats et les états « recherche en cours », « aucun résultat » et « erreur » ne s'appuient QUE sur accessibilityLiveRegion="polite", propriété ANDROID-ONLY de React Native. Sur iOS, VoiceOver n'annonce rien : les cinq états sont muets au lecteur d'écran, y compris l'erreur (accessibilityRole="alert" n'auto-annonce pas non plus sur iOS en RN). Ce n'est pas une fatalité du dépôt : AccessibilityInfo.announceForAccessibility est déjà utilisé ailleurs pour exactement ce besoin, avec le commentaire « équivalence d'information : reduce-motion et lecteur d'écran reçoivent le même fait ».

PREUVE : customer-form.tsx:212 (SuggestionList), :229 (SuggestionSkeleton), :658 (searching), :700 (empty) — uniquement accessibilityLiveRegion. Précédent contraire dans le même dépôt : apps/mobile/app/equipements/[chantierId].tsx:237 AccessibilityInfo.announceForAccessibility(t('equipements.createdAnnounce', …)), idem app/notifications.tsx, app/contrat/[id].tsx, src/components/GlobalBobAccess.tsx.

### [MOYENNE] apps/api/src/adapters/recherche-entreprises.adapter.ts
Le cooldown 429 est un champ STATIQUE partagé entre la recherche et le lookup — présenté comme une qualité, c'est ici un amplificateur. La nouvelle route PUBLIQUE et non authentifiée GET /company/search (30/min/IP, per_page=10, appelée en régime d'autocomplétion) partage le budget amont de 7 req/s attaché à l'IP de SORTIE du serveur, c'est-à-dire à TOUS les tenants réunis. Un unique 429 amont (déclenché par une poignée d'utilisateurs qui tapent en même temps, ou par un anonyme qui martèle la route — voir la finding sur onSubmitEditing) éteint l'annuaire ENTIER pour tout le processus pendant Retry-After, lookup SIRET compris : l'inscription (étape SIRET) tombe pour tout le monde. Aucun étranglement côté serveur vers l'amont (pas de seau à jetons global, pas de file) ; le commentaire affirme que « le débounce client, la garde de 3 caractères et le throttle de la route suffisent » — un throttle PAR IP CLIENTE ne protège pas un budget PAR IP SERVEUR.

PREUVE : Sonde : premier appel searchByName avec une réponse amont 429 (retry-after: 120) ⇒ CompanyLookupUnavailableError et cooldownUntil = now+120 s ; second appel via SearchCompaniesByName avec un fetch NEUF ⇒ fetch2.mock.calls.length === 0 et résultat {"ok":false,"error":{"kind":"dependency","port":"recherche-entreprises","cause":"Service entreprises temporairement indisponible (throttle)."}} — plus aucun appel n'est tenté, pour aucun chemin, pendant 120 s. adapter :46 `private static cooldownUntil = 0` ; backend.service.ts:1360-1367 une instance unique pour les deux ports.

### [FAIBLE] apps/mobile/src/components/customer-form.tsx
RÈGLE MÉTIER DANS UN COMPOSANT (point 8) — la règle « l'établissement n'est envoyé que s'il a été RÉSOLU par l'annuaire » est écrite en ligne dans submit() (`siret.length === 14 && !siretCandidateUnverified`) et n'existe nulle part ailleurs : ni dans company-search-field.logic.ts (le module pur créé exactement pour ça, 253 lignes, 326 lignes de tests), ni dans @bob/core. Elle est donc NON TESTÉE, et c'est précisément elle qui décide si une identité légale part au serveur. Même remarque, moindre, pour SEARCH_DEBOUNCE_MS = 350 laissé en constante de fichier alors que COMPANY_SEARCH_MIN_QUERY_LENGTH et COMPANY_SEARCH_MAX_RESULTS sont, eux, exportés depuis les ports.

PREUVE : customer-form.tsx:571 (ligne AJOUTÉE par le lot, cf. diff origin/main...HEAD). `grep -n "siretCandidateUnverified\|length === 14" apps/mobile/src/components/company-search-field.logic.ts` => aucun résultat ; `grep -rn "siret.length === 14" packages/core/src` => aucun résultat.

### [FAIBLE] packages/i18n/src/index.ts
Trois clés i18n ajoutées par le lot sont MORTES : clients.createCompanySearchClear (« Effacer la recherche » — il n'existe aucun bouton d'effacement dans le champ, alors que c'est le geste de sortie évident d'une recherche verrouillée), clients.createIdentityReplaceCta (« Remplacer ») et clients.createIdentityReplaceCancel (« Laisser comme ça » / « Annuler »). Conséquence visible pour ces deux dernières : la feuille de confirmation du remplacement d'identité (pickCompany → confirm({title, message, challenge})) affiche ses libellés PAR DÉFAUT, pas ceux que le lot a rédigés — la question posée est « Je remplace l'entreprise de cette fiche ? » et le bouton ne dit pas « Remplacer ».

PREUVE : git grep -n 'createCompanySearchClear' -- 'apps/**' 'packages/**' ':!packages/i18n/src/index.ts' => 0 résultat ; idem createIdentityReplaceCta et createIdentityReplaceCancel. customer-form.tsx:413-419 : l'appel confirm() ne passe que title, message et challenge.

======================================================================
REVUE 2
======================================================================

### [BLOQUANTE] apps/api/src/main.ts
La route POST /company/identity-extraction n'a pas été ajoutée à LARGE_JSON_PAYLOAD_ROUTES (main.ts l.24-32). Elle hérite donc de DEFAULT_JSON_BODY_LIMIT = '256kb', soit ~192 ko d'image réellement acceptés une fois le base64 pris en compte — 21,3× moins que les 4 Mo annoncés par COMPANY_IDENTITY_MAX_BYTES, garantis par le domaine (extract-company-identity.ts l.334) et pré-contrôlés côté mobile par exceedsPhotoBudget. Une photo d'appareil à `quality: 0.6` pèse 0,5 à 3 Mo ; le commentaire du lot admet lui-même « moins d'1 Mo », soit déjà 5× l'enveloppe. Conséquence : le geste central (« prends l'en-tête en photo ») échoue en HTTP 413 avant le guard, le throttle et le contrôleur. Et le motif affiché est FAUX : le corps d'un 413 ne porte pas d'AppError, http-client.ts (l.2092) le convertit en {kind:'dependency', port:'api', cause:'HTTP 413'}, que companyIdentityErrorKey (company-identity-photo.logic.ts l.64) traduit en 'clients.photoEngineDown' — « le moteur de lecture ne répond pas ». L'artisan reprendra la photo indéfiniment. Doctrine 8 (motif nommé) et doctrine 4 (une panne se DIT).

PREUVE : Test exécuté dans le worktree (apps/api, vitest) : usesLargeJsonBodyParser({method:'POST', url:'/company/identity-extraction', headers:{'content-type':'application/json'}}) === false, usesDefaultJsonBodyParser(...) === true, DEFAULT_JSON_BODY_LIMIT === '256kb' — 2 tests passés. Calcul reproductible : enveloppe 262144 o ; overhead JSON {"contentBase64":"","mimeType":"image/jpeg"} = 44 o ; image max = floor((262144-44)*3/4) = 196575 o = 192 ko. Corps JSON pour une photo de 1 Mo = 1 398 146 o >> 262 144 o → 413. À comparer : /documents/ocr et /chantiers/:id/photos, qui transportent le même genre de charge, SONT dans la liste.

### [MAJEURE] apps/mobile/src/components/company-identity-photo.logic.ts
REFUS SILENCIEUX (doctrine 8, non négociable). Cas d'un en-tête français ordinaire portant « SIRET … — TVA FR… » sans SIREN imprimé, quand l'annuaire ne confirme pas (not_found, ou unavailable — ce qui arrive à CHAQUE fenêtre de cooldown 429 process-wide de l'adaptateur). Le domaine dérive alors le SIREN arithmétiquement du SIRET (retainedSiren, extract-company-identity.ts l.236), la TVA passe la garde de clé et devient une rangée sélectionnable « lu sur la photo, pas vérifié ». Mais aucune rangée SIREN n'existe (champ non lu ⇒ rowOf rend null l.137). toggleIdentitySelection (l.220) ne peut donc pas cocher le SIREN lié, et identityFormPatch (l.277) applique la garde `tvaIntracom === null || siren === null ? {} : {tvaIntracom}` : la TVA est SUPPRIMÉE du patch. L'utilisateur coche la TVA, appuie sur « Appliquer », et il ne se passe rien — aucun message, aucun motif, aucune trace. Le commentaire l.275 dit vouloir éviter de bloquer « Enregistrer » ; il a créé un refus muet à la place.

PREUVE : Test exécuté (apps/mobile, vitest, 4 tests passés) : draft{siret:'45132133501021', tvaIntracom:'FR90451321335'} + lookup qui throw ⇒ identityRows = ['siret','tvaIntracom'] ; la rangée tvaIntracom est {value:'FR90451321335', selectable:true, verified:false, rejectionKey:null} ; aucune rangée 'siren' ; après toggleIdentitySelection(..., 'tvaIntracom', rows), selected.has('tvaIntracom') === true ; identityFormPatch(...) rend patch.tvaIntracom === undefined et Object.keys(patch) === ['siretVerified'].

### [MAJEURE] packages/core/src/application/company/extract-company-identity.ts
retainedSiren (l.233-237) préfère le SIREN LU PAR LE MODÈLE (`if (read.siren.value !== null) return read.siren.value`) à celui EXTRAIT d'un SIRET Luhn-valide lu sur la même photo — c'est-à-dire la source la MOINS fiable devant celle que le code lui-même qualifie d'« extraction arithmétique INSEE, pas une hypothèse » (l.230-231). Deux conséquences. (a) Une contradiction déterministe et détectable — siret.slice(0,9) ≠ siren, alors que Customer.of la traite en « corruption d'identité, jamais un cas métier » (customer.ts l.196) — n'est ni signalée ni refusée : les deux rangées sont proposées côte à côte, sélectionnables, sans motif. (b) Elle offre au modèle un levier : en fabriquant un SIREN, il fabrique le référentiel qui valide sa propre TVA. Le n° de TVA inventé passe alors la garde de cohérence et s'affiche avec une clé qui « tombe juste », tandis que le vrai n° de TVA de l'entreprise réellement lue est, lui, REFUSÉ pour incohérence. Le rejet nommé 'vat_inconsistent' existe déjà : il suffirait de l'appliquer aussi au couple SIREN/SIRET.

PREUVE : Test exécuté (packages/core, vitest, 7 tests passés). Entrée : siret='45132133501021' (SIREN réel 451321335), siren='552081317', tvaIntracom='FR03552081317', annuaire qui throw. Sortie : siren = {value:'552081317', status:'unverified', rejection:null}, siret = {value:'45132133501021', status:'unverified', rejection:null} — aucune alerte sur la contradiction — et tvaIntracom = {value:'FR03552081317', status:'unverified', rejection:null}. Contre-épreuve, même entrée mais tvaIntracom='FR90451321335' (le VRAI numéro du SIRET lu) : rejection === 'vat_inconsistent'.

### [MAJEURE] packages/core/src/application/ports/company-identity-extraction.ts
RÉPONSE À LA QUESTION 1 : OUI, un champ illisible peut être inventé par le modèle ET présenté comme lu. Le port déclare (l.28-38) que `excerpt` « porte le fragment exact du document où la valeur a été lue : sans lui, l'humain validerait à l'aveugle », et le prompt système (company-identity-extraction.ts l.66) ordonne « Sans extrait, mets le champ à null ». Mais AUCUN code ne vérifie quoi que ce soit : readField (extract-company-identity.ts l.142-156) assainit la valeur et l'extrait indépendamment, sans jamais contrôler que la valeur figure dans son extrait, ni même qu'un extrait existe. Une valeur avec extrait null est acceptée telle quelle ; une valeur avec un extrait qui parle d'une AUTRE entreprise est acceptée telle quelle. La « preuve » affichée sous chaque rangée (CompanyIdentityReviewPanel.tsx l.143-147, 'clients.photoFieldExcerpt') est donc une preuve auto-décernée par la partie dont on se méfie. Un modèle qui hallucine hallucine aussi son extrait — c'est le comportement attendu d'un LLM, pas un cas d'école. Le contrôle déterministe manquant est trivial (la valeur normalisée doit apparaître dans l'extrait normalisé) et transformerait un champ non prouvé en 'rejected' avec motif nommé.

PREUVE : Test exécuté (packages/core, vitest) : draft{siret:{value:'73282932000074', excerpt:'SIRET : 45132133501021'}, denomination:{value:'MENUISERIE DUPONT SARL', excerpt:'Société Générale — agence de Rouen'}} ⇒ la revue conserve siret.value='73282932000074' AVEC excerpt='SIRET : 45132133501021', et denomination.value='MENUISERIE DUPONT SARL' AVEC excerpt='Société Générale — agence de Rouen'. Second cas : draft{denomination:{value:'ENTREPRISE FANTÔME', excerpt:null}} ⇒ {value:'ENTREPRISE FANTÔME', status:'unverified', excerpt:null} — la règle « sans extrait ⇒ null » n'existe que dans le prompt.

### [MAJEURE] apps/mobile/src/components/customer-form.tsx
Le défi de remplacement d'identité est contournable. applyPhotoPatch (l.523) ne déclenche le confirm() que si `patch.siren !== undefined && replacesExistingIdentity(siren, patch.siren)` — le garde-fou est indexé sur le seul SIREN. Or dans le panneau de relecture, SIRET et SIREN sont deux rangées INDÉPENDAMMENT décochables : toggleIdentitySelection couple TVA↔SIREN (l.220-221) mais jamais SIRET↔SIREN. En édition d'un client déjà facturé, décocher le SIREN et garder le SIRET écrit un nouvel ÉTABLISSEMENT LÉGAL sans aucune confirmation, alors que le commentaire du même bloc affirme « remplacer l'identité légale d'un client déjà facturé ne doit jamais être l'effet de bord d'une photo ». Pire, cette combinaison est exactement celle que Customer.of refuse en aval (customer.ts l.196 : « Le SIRET et le SIREN de ce client ne concordent pas ») : l'utilisateur n'obtient donc ni la confirmation qui l'aurait protégé, ni un blocage local, mais une erreur serveur à l'enregistrement.

PREUVE : Test exécuté (apps/mobile, vitest) : annuaire CONFIRMÉ sur 45132133501021 ⇒ initialIdentitySelection(r).has('siren') === true (tout est pré-coché) ; après toggleIdentitySelection(sel, 'siren', rows) ⇒ selected.has('siren') === false et selected.has('siret') === true ; identityFormPatch rend {siret:'45132133501021', siretVerified:true} avec patch.siren === undefined ⇒ la condition l.523 est fausse ⇒ writePhotoPatch est appelé directement, sans confirm().

### [MOYENNE] apps/mobile/src/components/CompanyIdentityReviewPanel.tsx
La divergence et l'extrait sont INAUDIBLES au lecteur d'écran. RowView (l.86-111) est un Pressable — donc `accessible` vaut true par défaut en React Native — porteur d'un accessibilityLabel explicite ('clients.photoRowA11y', params label/value/status). Ce label REMPLACE le texte des enfants dans l'arbre d'accessibilité (le fichier en est conscient : CheckMark s'auto-masque avec accessibilityElementsHidden l.40-42). Or les deux Text les plus informatifs de la rangée sont des enfants : divergentPhotoValue l.135-142 (« Sur la photo, c'était “Carrefour Fécamp” » — le signal qui révèle qu'un modèle a lu autre chose que l'annuaire) et excerpt l.143-147 (la preuve de lecture). Un utilisateur non-voyant entend le statut mais jamais la divergence, c'est-à-dire précisément ce que l'en-tête du fichier promet : « La différence entre “confirmé par l'annuaire” et “lu sur la photo” doit sauter aux yeux — et s'ENTENDRE ». Doctrine 5. Le motif correct existe à trente lignes de là, dans le même lot : customer-form.tsx l.678-684 plie `details` dans 'clients.createCompanyRowA11y'.

PREUVE : Lecture de code. React Native : Pressable rend `accessible={accessible !== false}`, donc true ici ; avec un accessibilityLabel sur un nœud accessible, VoiceOver/TalkBack annoncent le label et non la concaténation des enfants. Les paramètres passés à 'clients.photoRowA11y' (l.90-93) sont exactement {label, value, status} — ni excerpt, ni divergentPhotoValue. Vérifiable en comparant à customer-form.tsx l.678, où `details` EST injecté dans le label.

### [MOYENNE] design_handoff_bob_pro/MATRICE_FLAGS_V1.md
COMPANY_IDENTITY_EXTRACTION_ENABLED n'a pas été ajouté au bloc machine-readable de la matrice des flags V1, alors que le code s'auto-désigne comme porte de publication : env.ts l.1268-1272 écrit « PORTE DE PUBLICATION : ne pas passer ce drapeau à 'true' en production tant que la ligne “Fournisseur d'extraction OCR” … porte encore [EN ATTENTE] ». Cette consigne n'a donc AUCUN garde-fou automatisé. Le test flags-matrix-v1.test.ts existe précisément pour ça — son commentaire l.74-78 dit : « sans elle, supprimer silencieusement une entrée du bloc (puis changer le défaut dans env.ts) laisserait la suite verte. Toute évolution de cette liste = amender la matrice ET ce test dans le même commit ». Deux flags booléens strictement analogues y sont déjà figés (VOICE_TRACE_ENABLED, FISCAL_PUBLICODES_SIMULATIONS_ENABLED). Le seul drapeau qui commande l'envoi du document d'un TIERS à un sous-traitant dont la localisation est encore [EN ATTENTE] est celui qui échappe au verrou anti-drift.

PREUVE : git diff main...HEAD -- design_handoff_bob_pro/MATRICE_FLAGS_V1.md : aucune entrée de scope 'api' ajoutée (le diff ne touche que les six lignes EXPO_PUBLIC_* mobile-preview/production). EXPECTED_API_FLAG_NAMES (flags-matrix-v1.test.ts l.80-111) liste 30 noms, COMPANY_IDENTITY_EXTRACTION_ENABLED n'y figure pas ; l'assertion l.213 compare les noms de la matrice à cette liste — elle reste verte parce que ni l'une ni l'autre n'a été amendée.

### [MOYENNE] apps/mobile/src/components/customer-form.tsx
RGPD, question 3 : côté serveur le contrat de non-stockage tient (le use case ne reçoit aucun port de stockage, l'audit ne journalise que des compteurs, aucun logger ne touche au corps). Côté APPAREIL en revanche, la photo n'est jamais supprimée. pickHeaderPhoto (l.435-484) appelle ImagePicker.launchCameraAsync/launchImageLibraryAsync avec base64:true : Expo copie l'image dans le répertoire de cache de l'app et ne la supprime pas ; asset.uri n'est ni lu ni effacé après l'envoi. La photo de l'en-tête d'un TIERS reste donc sur le téléphone sine die, alors que la politique de confidentialité écrite par ce lot même affirme sans réserve « L'image n'est jamais conservée. Elle est lue puis relâchée immédiatement » (§2.5 bis). Le dépôt connaît pourtant le geste exact pour un média éphémère : apps/mobile/src/data/voice.ts l.345, 427, 473, 555 appelle deleteAsync(uri, {idempotent:true}) après transcription.

PREUVE : grep -rn 'deleteAsync|FileSystem' sur apps/mobile/src et apps/mobile/app : 8 occurrences, toutes dans src/data/voice.ts ; aucune dans customer-form.tsx. Le corps de pickHeaderPhoto n'utilise que asset.base64 et asset.mimeType (l.461-475) — asset.uri n'est jamais référencé, donc jamais nettoyé.

### [MINEURE] apps/mobile/src/components/company-identity-photo.logic.ts
companyIdentityErrorKey (l.66-69) écrase trois causes distinctes sous une seule phrase, et cette phrase est fausse pour deux d'entre elles : tout AppError 'validation'/'domain' dont le premier champ n'est pas 'mimeType' rend 'clients.photoTooBig' = « Image trop volumineuse (4 Mo maximum) ». Or decodeBase64Document (backend.service.ts l.848-905) émet aussi, sur le champ contentBase64, « Base64 invalide. » et « Document vide. » — deux causes qui n'ont rien à voir avec le poids, et pour lesquelles l'utilisateur s'entendra dire de réduire une image qui est en fait vide ou corrompue. Le refus porte bien un motif serveur nommé ; c'est la traduction cliente qui le renomme à tort. Faiblement atteignable depuis l'app (asset.base64 est contrôlé non vide l.461), pleinement atteignable pour tout autre client de l'API.

PREUVE : Lecture de code : le switch l.59-71 ne discrimine que validationField(error) === 'mimeType' ; toutes les autres valeurs de field tombent sur 'clients.photoTooBig'. Copies i18n (packages/i18n/src/index.ts l.1434-1437) : pro = « Image trop volumineuse (4 Mo maximum). Reprenez la photo. » Messages serveur concernés : backend.service.ts l.858 'Base64 invalide.', l.888 'Document vide.', et extract-company-identity.ts l.332 'Image vide.'

### [MINEURE] packages/core/src/application/company/extract-company-identity.ts
vatField (l.250-252) attribue à `photoValue` un numéro de TVA qui vient de l'ANNUAIRE et non de la photo : `candidate = published ?? read.tvaIntracom.value`, puis en cas de refus `rejected('tvaIntracom', read.tvaIntracom, ..., candidate)`. Quand rien n'a été lu sur l'image et que c'est le numéro publié par l'annuaire qui échoue à la vérification de clé, l'écran affiche « Sur la photo, c'était “FR01451321335” » (clients.photoFieldPhotoValue) pour une valeur que la photo n'a jamais portée. C'est une valeur inventée présentée comme lue — l'inverse exact du contrat du champ, documenté l.62-67 (« Valeur LUE SUR LA PHOTO »). Le cas est rare en pratique (l'annuaire calcule la clé depuis le SIREN) mais le champ ment par construction, pas par accident.

PREUVE : Test exécuté (packages/core, vitest) : draft{siret:{value:'45132133501021'}} — aucune TVA lue — avec un annuaire renvoyant tvaIntracom:'FR01451321335' (clé fausse) ⇒ identityField(review,'tvaIntracom') = {status:'rejected', photoValue:'FR01451321335'}. read.tvaIntracom.value valait null.

### [MINEURE] packages/core/src/application/company/extract-company-identity.ts
sanitizeText (l.114-127) ne connaît que la longueur : « lu mais vide n'existe pas » n'est vrai que pour la chaîne vide littérale. Les non-valeurs qu'un modèle produit spontanément quand un champ est absent — « N/A », « - », « néant », « non communiqué » — traversent intactes, deviennent des rangées sélectionnables et sont COMPTÉES dans readCount, donc dans la phrase de synthèse « J'ai lu N information(s) sur la photo » (clients.photoReviewSummary). Cochées, elles s'écrivent telles quelles : « N/A » peut devenir la raison sociale d'un client. Les champs numériques sont protégés (sanitizeDigits écarte 'N/A' pour siret/siren/zip), les champs texte ne le sont pas. Le garde-fou du statut joue — c'est marqué « lu sur la photo, pas vérifié » — mais le compteur, lui, ment sur la quantité d'information réellement lue.

PREUVE : Tests exécutés (packages/core puis apps/mobile) : draft{denomination:'N/A', email:'N/A', phone:'-', city:'néant'} ⇒ denomination.value==='N/A', email.value==='N/A', phone.value==='-', et readCount === 4. Côté mobile : après toggleIdentitySelection(..., 'denomination', rows), identityFormPatch(...).companyName === 'N/A'.

### [MINEURE] apps/mobile/src/components/CompanyIdentityReviewPanel.tsx
Doctrine 2 (« papa vocal ») et 3 (parité humain↔Bob) : la relecture n'existe qu'au doigt. Une fois la photo lue, arbitrer champ par champ passe exclusivement par des cases à cocher (RowView, Pressable accessibilityRole='checkbox') puis un bouton « Appliquer ». L'artisan qui vient de photographier un en-tête, les mains sales, ne peut pas dire « garde le SIRET et l'adresse, oublie le mail » — et aucun outil agent n'expose cette capacité (packages/ai/src/tools/registry.ts n'ajoute que chercher_entreprise et creer_client, tous deux du lot A). Atténuation réelle et assumée : le repli 'clients.photoSearchByName' renvoie vers la recherche par raison sociale, elle, pleinement vocale. Reste que le geste principal du lot B est, en l'état, un geste qui ne réussit pas à la voix.

PREUVE : CompanyIdentityReviewPanel.tsx : les seuls points d'écriture sont onToggle l.251-253 (tap) et le Button 'clients.photoApply' l.269-276 (tap). git diff main...HEAD -- packages/ai/src/tools/registry.ts : les deux seuls outils ajoutés sont chercher_entreprise et l'extension SIRET de creer_client ; aucune action n'expose POST /company/identity-extraction ni la sélection de champs.
